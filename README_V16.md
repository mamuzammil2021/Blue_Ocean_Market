Blue Ocean Market V16

This rebuild fixes the shipped UI so public/index.html loads the actual client.js that contains the current dashboard/workspace implementation. Dashboard is strictly scoped by the top Workspace selector. MIMI has one dashboard and Restaurant Tables is separate from POS. Excavator has one dashboard and a persistent machine lifecycle.
